# UFO³ Galaxy UI 设计指南

## 设计理念：黑白渐变极简极客风格

本文档详细说明 UFO³ Galaxy 系统的 UI 设计理念和实现细节。

---

## 一、设计原则

### 1. 极简主义（Minimalism）
- **去除一切不必要的元素**
- **只保留核心功能**
- **让用户专注于任务本身**

### 2. 极客美学（Geek Aesthetics）
- **黑白配色**：象征代码和终端
- **等宽字体**：Consolas/Monospace
- **几何图形**：直线、圆形、网格

### 3. 科技感（Tech Feel）
- **渐变背景**：从纯黑到深灰
- **动画效果**：流畅的滑动和淡入淡出
- **状态指示**：实时的连接状态和时间显示

---

## 二、Windows 侧边栏 UI

### 设计特点

| 特性 | 说明 |
|:---|:---|
| **位置** | 屏幕右侧，默认隐藏 |
| **唤起方式** | 按 F12 键（模拟 Fn 键） |
| **尺寸** | 宽 400px，高度占满屏幕 |
| **透明度** | 95% 不透明 |
| **动画** | 20 步滑动动画，每步 10ms |

### 配色方案

```
背景渐变：
  - 顶部：#000000（纯黑）
  - 底部：#1a1a1a（深灰）

文字颜色：
  - 主标题：#ffffff（纯白）
  - 副标题：#888888（中灰）
  - 提示文字：#555555（深灰）

强调色：
  - 成功：#00ff00（绿色）
  - 错误：#ff0000（红色）
  - 警告：#ffff00（黄色）

装饰线条：
  - 主线条：#ffffff（纯白）
  - 网格线：#333333（深灰，半透明）
```

### 布局结构

```
┌─────────────────────────────────┐
│  UFO³ GALAXY          [_][×]   │ ← 标题栏（60px）
├─────────────────────────────────┤
│  > INPUT COMMAND                │
│  ┌───────────────────────────┐ │
│  │ 输入框（3 行）             │ │ ← 输入区域（120px）
│  └───────────────────────────┘ │
│  [EXECUTE [ENTER]]              │
├─────────────────────────────────┤
│  > MESSAGE HISTORY              │
│  ┌───────────────────────────┐ │
│  │                            │ │
│  │  消息历史                  │ │ ← 消息区域（自适应）
│  │  （滚动）                  │ │
│  │                            │ │
│  └───────────────────────────┘ │
├─────────────────────────────────┤
│  ● CONNECTED      12:34:56     │ ← 状态栏（60px）
│  Press F12 to toggle panel     │
└─────────────────────────────────┘
```

### 核心代码

```python
# 黑白渐变背景
for i in range(self.height):
    ratio = i / self.height
    gray_value = int(26 * ratio)  # 从 0 到 26
    color = f'#{gray_value:02x}{gray_value:02x}{gray_value:02x}'
    self.canvas.create_line(0, i, self.width, i, fill=color)

# 滑动动画
def _animate_slide(self, start_x: int, end_x: int):
    steps = 20
    delta = (end_x - start_x) / steps
    
    def animate(step=0):
        if step < steps:
            current_x = int(start_x + delta * step)
            self.root.geometry(f"{self.width}x{self.height}+{current_x}+0")
            self.root.after(10, lambda: animate(step + 1))
    
    animate()
```

---

## 三、Android 悬浮窗 UI

### 设计特点

| 特性 | 说明 |
|:---|:---|
| **位置** | 屏幕右上角，可拖动 |
| **尺寸** | 宽 320dp，高 480dp（展开时） |
| **折叠** | 可折叠为 60dp 高的小窗口 |
| **圆角** | 16dp 圆角 |
| **阴影** | 8dp 阴影 |

### 配色方案

与 Windows 版本保持一致，但针对移动端进行了优化：

```
背景渐变：
  - 顶部：#000000（纯黑）
  - 底部：#1a1a1a（深灰）

按钮颜色：
  - 主按钮：#ffffff（白底黑字）
  - 次按钮：#2a2a2a（深灰底）
  - 语音按钮：#2a2a2a（深灰底）

输入框：
  - 背景：#1a1a1a（深灰）
  - 文字：#ffffff（纯白）
  - 提示：#555555（深灰）
```

### 布局结构

```
┌─────────────────────────────┐
│ ● UFO³ GALAXY      [^][×]  │ ← 标题栏（60dp）
├─────────────────────────────┤
│ > INPUT COMMAND             │
│ ┌─────────────────────────┐ │
│ │ 输入框               │🎤│>│ │ ← 输入区域（60dp）
│ └─────────────────────────┘ │
├─────────────────────────────┤
│ > MESSAGE HISTORY           │
│ ┌─────────────────────────┐ │
│ │                          │ │
│ │  消息历史                │ │ ← 消息区域（自适应）
│ │  （滚动）                │ │
│ │                          │ │
│ └─────────────────────────┘ │
├─────────────────────────────┤
│ READY                       │ ← 状态栏（40dp）
└─────────────────────────────┘
```

### 核心代码

```kotlin
// 黑白渐变背景
private fun createGradientBackground(): Drawable {
    return object : Drawable() {
        private val paint = Paint().apply {
            isAntiAlias = true
        }
        
        override fun draw(canvas: Canvas) {
            val bounds = bounds
            
            // 绘制黑白渐变
            val gradient = LinearGradient(
                0f, 0f,
                0f, bounds.height().toFloat(),
                Color.parseColor("#000000"),
                Color.parseColor("#1a1a1a"),
                Shader.TileMode.CLAMP
            )
            paint.shader = gradient
            canvas.drawRect(bounds, paint)
            
            // 绘制几何装饰线条
            paint.shader = null
            paint.color = Color.parseColor("#333333")
            paint.strokeWidth = dpToPx(1).toFloat()
            paint.alpha = 100
            
            // 斜线网格
            for (i in 0 until bounds.width() step dpToPx(50)) {
                canvas.drawLine(
                    i.toFloat(), 0f,
                    (i + dpToPx(100)).toFloat(), dpToPx(100).toFloat(),
                    paint
                )
            }
        }
        
        // ...
    }
}
```

---

## 四、交互设计

### Windows 侧边栏

| 操作 | 触发方式 | 效果 |
|:---|:---|:---|
| **显示/隐藏** | 按 F12 键 | 从右侧滑入/滑出 |
| **发送消息** | 按 Enter 键或点击按钮 | 发送命令到 Galaxy |
| **关闭面板** | 点击右上角 × | 隐藏面板 |

### Android 悬浮窗

| 操作 | 触发方式 | 效果 |
|:---|:---|:---|
| **拖动** | 长按标题栏拖动 | 移动悬浮窗位置 |
| **展开/折叠** | 点击 ^ 按钮 | 切换展开/折叠状态 |
| **发送消息** | 点击 > 按钮 | 发送命令到 Galaxy |
| **语音输入** | 点击 🎤 按钮 | 启动语音识别 |
| **关闭** | 点击 × 按钮 | 隐藏悬浮窗 |

---

## 五、状态指示

### 连接状态

| 状态 | 指示器颜色 | 文字 |
|:---|:---:|:---|
| **已连接** | 🟢 绿色 | CONNECTED |
| **未连接** | 🔴 红色 | DISCONNECTED |
| **连接中** | 🟡 黄色 | CONNECTING |

### 消息类型

| 类型 | 颜色 | 示例 |
|:---|:---:|:---|
| **用户消息** | #ffffff（纯白，粗体） | [USER] 打印一个花瓶 |
| **系统消息** | #888888（中灰） | [SYSTEM] Connected to Galaxy |
| **成功消息** | #00ff00（绿色） | [SUCCESS] Task completed |
| **错误消息** | #ff0000（红色） | [ERROR] Connection failed |

---

## 六、字体和排版

### 字体选择

- **Windows**：Consolas（等宽字体）
- **Android**：Monospace（系统等宽字体）

### 字号规范

| 用途 | Windows | Android |
|:---|:---:|:---:|
| **主标题** | 18pt | 14sp |
| **副标题** | 9pt | 10sp |
| **正文** | 11pt | 12sp |
| **消息历史** | 10pt | 10sp |
| **状态栏** | 9pt | 9sp |
| **提示文字** | 8pt | 8sp |

---

## 七、动画效果

### 滑动动画

- **持续时间**：200ms
- **缓动函数**：线性（Linear）
- **步数**：20 步

### 淡入淡出

- **持续时间**：150ms
- **透明度变化**：0% → 95%

### 状态指示器闪烁

- **周期**：1 秒
- **效果**：连接中时闪烁黄色

---

## 八、响应式设计

### Windows

- **最小宽度**：400px
- **最小高度**：600px
- **适配**：自动适配屏幕高度

### Android

- **最小宽度**：280dp
- **最小高度**：400dp
- **适配**：
  - 小屏幕（< 5 英寸）：宽度 280dp
  - 中等屏幕（5-6 英寸）：宽度 320dp
  - 大屏幕（> 6 英寸）：宽度 360dp

---

## 九、无障碍设计

### 对比度

- **文字与背景对比度**：≥ 7:1（WCAG AAA 级别）
- **按钮与背景对比度**：≥ 4.5:1（WCAG AA 级别）

### 键盘导航

- **Tab 键**：在输入框和按钮之间切换
- **Enter 键**：发送消息
- **Esc 键**：关闭面板

### 屏幕阅读器

- 所有按钮和输入框都有明确的标签
- 状态变化会通过屏幕阅读器通知用户

---

## 十、实现文件

### Windows

- **主文件**：`enhancements/clients/windows_client/client_ui_minimalist.py`
- **依赖**：
  - `tkinter`（Python 标准库）
  - `keyboard`（键盘监听）
  - `websockets`（WebSocket 通信）

### Android

- **主文件**：`enhancements/clients/android_client/app/src/main/java/com/ufo/galaxy/ui/MinimalistFloatingWindow.kt`
- **依赖**：
  - `androidx.cardview:cardview`
  - `androidx.constraintlayout:constraintlayout`

---

## 十一、使用指南

### Windows

1. 安装依赖：
   ```bash
   pip install keyboard websockets
   ```

2. 运行客户端：
   ```bash
   python client_ui_minimalist.py
   ```

3. 按 F12 键唤起侧边栏

### Android

1. 构建 APK：
   ```bash
   ./gradlew assembleDebug
   ```

2. 安装到手机：
   ```bash
   adb install app/build/outputs/apk/debug/app-debug.apk
   ```

3. 授予悬浮窗权限

4. 启动应用，悬浮窗自动显示

---

## 十二、未来优化

### 短期（1-2 周）

- [ ] 添加更多动画效果
- [ ] 支持自定义主题颜色
- [ ] 添加快捷键配置

### 中期（1-2 月）

- [ ] 支持多显示器
- [ ] 添加语音波形动画
- [ ] 支持手势操作（Android）

### 长期（3-6 月）

- [ ] 完全自定义 UI
- [ ] 支持插件系统
- [ ] 跨平台统一设计语言

---

**设计师**：Manus AI  
**日期**：2025-01-20  
**版本**：v1.0
