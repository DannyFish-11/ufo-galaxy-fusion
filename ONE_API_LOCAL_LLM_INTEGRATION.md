# One-API + Local LLM 深度集成

## 🎯 核心价值

将 **Node 01 (One-API Gateway)** 和 **Node 79 (Local LLM)** 深度集成，实现：

1. ✅ **统一调用接口** - 所有模型通过 One-API 调用
2. ✅ **智能路由** - 根据策略自动选择本地/云端
3. ✅ **自动 Fallback** - 本地失败自动切换云端
4. ✅ **成本优化** - 本地推理零成本
5. ✅ **透明切换** - 用户无感知

---

## 📐 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    应用层 (Your App)                         │
│                                                              │
│  调用: POST http://localhost:8001/v1/chat/completions       │
│        { "model": "auto", "messages": [...] }               │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              Node 01: One-API Gateway (8001)                │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │            智能路由策略                               │  │
│  │                                                       │  │
│  │  1. 本地优先模式 (LOCAL_LLM_PRIORITY=1)              │  │
│  │     └─> 本地成功 ✓ → 返回                            │  │
│  │     └─> 本地失败 ✗ → Fallback 云端                   │  │
│  │                                                       │  │
│  │  2. 云端优先模式 (LOCAL_LLM_PRIORITY=0)              │  │
│  │     └─> 云端成功 ✓ → 返回                            │  │
│  │     └─> 云端失败 ✗ → Fallback 本地                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  支持的提供商:                                               │
│  - local      (Node 79, 免费, 快速)                         │
│  - groq       (Llama 3.3, 免费, 快速)                       │
│  - zhipu      (GLM-4, 中文优秀)                             │
│  - openrouter (多模型聚合)                                   │
│  - claude     (Claude 3.5, 强大)                            │
└──────┬───────────────────────────────────────────┬──────────┘
       │                                           │
       │ 本地调用                                   │ 云端调用
       ▼                                           ▼
┌──────────────────────────┐          ┌──────────────────────┐
│  Node 79: Local LLM      │          │   云端 LLM API       │
│  (8079)                  │          │                      │
│                          │          │  - Groq API          │
│  ┌────────────────────┐  │          │  - 智谱 AI           │
│  │ OpenAI 兼容 API    │  │          │  - OpenRouter        │
│  │ /v1/chat/completions│ │          │  - Claude API        │
│  └─────────┬──────────┘  │          └──────────────────────┘
│            │              │
│            ▼              │
│  ┌────────────────────┐  │
│  │  Ollama Engine     │  │
│  │                    │  │
│  │  - DeepSeek-Coder  │  │
│  │  - Qwen2.5-14B     │  │
│  │  - Qwen2.5-7B      │  │
│  │  - Qwen2.5-3B      │  │
│  └────────────────────┘  │
└──────────────────────────┘
```

---

## 🚀 使用方式

### 1. 启动服务

```bash
# 1. 启动 Ollama
ollama serve

# 2. 下载模型
ollama pull qwen2.5:7b-instruct-q4_K_M
ollama pull deepseek-coder:6.7b-instruct-q4_K_M

# 3. 启动 Node 79 (Local LLM)
cd nodes/Node_79_LocalLLM
python main.py  # 运行在 8079 端口

# 4. 启动 Node 01 (One-API)
cd nodes/Node_01_OneAPI
python main.py  # 运行在 8001 端口
```

### 2. 配置环境变量

```bash
# .env 文件

# ===== 本地 LLM 配置 =====
LOCAL_LLM_ENABLED=true                    # 启用本地 LLM
LOCAL_LLM_URL=http://localhost:8079       # Node 79 地址
LOCAL_LLM_PRIORITY=1                      # 优先级 (1=最高)

# ===== 云端 API Key (可选) =====
GROQ_API_KEY=your_groq_key
ZHIPU_API_KEY=your_zhipu_key
OPENROUTER_API_KEY=your_openrouter_key
CLAUDE_API_KEY=your_claude_key
```

### 3. 调用示例

#### 方式 A: 自动选择（推荐）

```python
import requests

# One-API 会自动选择最优提供商
response = requests.post(
    "http://localhost:8001/v1/chat/completions",
    json={
        "model": "auto",  # 自动选择
        "messages": [
            {"role": "user", "content": "写一个 Python 快速排序"}
        ]
    }
)

result = response.json()
print(result["choices"][0]["message"]["content"])
```

**执行流程：**
1. One-API 检测到 `model="auto"`
2. 根据 `LOCAL_LLM_PRIORITY` 决定策略
3. 如果 `LOCAL_LLM_PRIORITY=1`：
   - 先调用 Node 79 (本地)
   - 本地成功 → 返回结果 ✅
   - 本地失败 → Fallback 到 Groq/智谱等云端 ✅

#### 方式 B: 指定本地模型

```python
# 直接指定使用本地 LLM
response = requests.post(
    "http://localhost:8001/v1/chat/completions",
    json={
        "model": "local/qwen2.5:7b-instruct-q4_K_M",  # 明确指定本地
        "messages": [
            {"role": "user", "content": "解释量子计算"}
        ]
    }
)
```

#### 方式 C: 指定云端模型

```python
# 明确指定云端提供商
response = requests.post(
    "http://localhost:8001/v1/chat/completions",
    json={
        "model": "groq/llama-3.3-70b-versatile",  # 云端 Groq
        "messages": [
            {"role": "user", "content": "分析市场趋势"}
        ]
    }
)
```

#### 方式 D: 代码任务自动选择 DeepSeek

```python
# Node 79 会自动检测代码任务，使用 DeepSeek-Coder
response = requests.post(
    "http://localhost:8001/v1/chat/completions",
    json={
        "model": "auto",
        "messages": [
            {"role": "user", "content": "Write a function to reverse a string"}
        ]
    }
)
# 自动路由: One-API → Node 79 → DeepSeek-Coder
```

---

## 📊 性能对比

| 指标 | 云端 API | 本地 LLM | 提升 |
|------|---------|---------|------|
| **成本** | $0.001-0.01/次 | $0 | **100%** |
| **响应时间** | 2-5秒 | 1-3秒 | **40%** |
| **隐私** | 数据上传 | 本地处理 | **100%** |
| **可用性** | 依赖网络 | 离线可用 | **100%** |
| **并发** | 受限 | 本地硬件 | **取决于硬件** |

---

## 🎛️ 路由策略

### 策略 1: 本地优先（推荐）

**配置：**
```bash
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PRIORITY=1
```

**流程：**
```
请求 → One-API
  ├─> 尝试本地 LLM (Node 79)
  │   ├─> 成功 ✅ → 返回 (成本: $0, 速度: 快)
  │   └─> 失败 ✗ → Fallback 云端
  │       ├─> Groq (免费, 快)
  │       ├─> 智谱 (中文好)
  │       ├─> OpenRouter (全面)
  │       └─> Claude (强大)
  └─> 返回结果
```

**适用场景：**
- 日常开发和测试
- 成本敏感场景
- 隐私要求高
- 网络不稳定

### 策略 2: 云端优先

**配置：**
```bash
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PRIORITY=0
```

**流程：**
```
请求 → One-API
  ├─> 尝试云端 API
  │   ├─> 成功 ✅ → 返回 (质量: 高)
  │   └─> 失败 ✗ → Fallback 本地
  │       └─> Node 79 (Local LLM)
  └─> 返回结果
```

**适用场景：**
- 生产环境
- 质量要求高
- 复杂推理任务
- 本地硬件有限

### 策略 3: 仅本地

**配置：**
```bash
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PRIORITY=1
# 不配置任何云端 API Key
```

**适用场景：**
- 完全离线环境
- 零成本运行
- 数据不能外传

### 策略 4: 仅云端

**配置：**
```bash
LOCAL_LLM_ENABLED=false
# 配置云端 API Key
```

**适用场景：**
- 无本地 GPU
- 需要最强模型
- 不关心成本

---

## 🔧 高级配置

### 1. 自定义模型映射

在 Node 79 中配置：

```bash
# .env
CODE_MODEL=deepseek-coder:6.7b-instruct-q4_K_M
COMPLEX_MODEL=qwen2.5:14b-instruct-q4_K_M
NORMAL_MODEL=qwen2.5:7b-instruct-q4_K_M
SIMPLE_MODEL=qwen2.5:3b-instruct-q4_K_M
```

### 2. 超时配置

```python
# Node 01 中调整
timeout=120  # 本地推理超时时间（秒）
```

### 3. 健康检查

```bash
# 检查 One-API 状态
curl http://localhost:8001/health

# 返回示例
{
  "status": "healthy",
  "available_providers": ["local", "groq", "zhipu"],
  "local_llm_enabled": true,
  "local_llm_priority": 1
}
```

### 4. 列出所有模型

```bash
curl http://localhost:8001/v1/models

# 返回示例
{
  "object": "list",
  "data": [
    {
      "id": "local/qwen2.5:7b-instruct-q4_K_M",
      "provider": "local",
      "cost": 0,
      "priority": 1
    },
    {
      "id": "groq/llama-3.3-70b-versatile",
      "provider": "groq",
      "cost": "free"
    },
    ...
  ]
}
```

---

## 💡 最佳实践

### 1. 开发环境

```bash
# 使用本地 LLM，降低成本
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PRIORITY=1

# 只配置一个免费的云端备用
GROQ_API_KEY=your_key
```

### 2. 生产环境

```bash
# 云端优先，保证质量
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PRIORITY=0

# 配置多个云端提供商
GROQ_API_KEY=your_key
ZHIPU_API_KEY=your_key
CLAUDE_API_KEY=your_key
```

### 3. 离线环境

```bash
# 仅本地
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PRIORITY=1
# 不配置任何云端 Key
```

### 4. 混合策略

```python
# 根据任务类型动态选择
def get_model(task_type):
    if task_type == "code":
        return "local/deepseek-coder:6.7b-instruct-q4_K_M"  # 本地代码模型
    elif task_type == "complex":
        return "claude/claude-3-5-sonnet-20241022"  # 云端强大模型
    else:
        return "auto"  # 自动选择
```

---

## 🐛 故障排查

### 问题 1: 本地 LLM 不可用

**症状：**
```json
{"error": "Local LLM not enabled"}
```

**解决：**
```bash
# 1. 检查 Ollama 是否运行
ollama list

# 2. 检查 Node 79 是否启动
curl http://localhost:8079/health

# 3. 检查环境变量
echo $LOCAL_LLM_ENABLED
```

### 问题 2: 自动 Fallback 不工作

**症状：** 本地失败后没有切换云端

**解决：**
```bash
# 确保配置了至少一个云端 API Key
export GROQ_API_KEY=your_key

# 重启 Node 01
```

### 问题 3: 响应慢

**症状：** 本地推理超过 30 秒

**解决：**
```bash
# 1. 使用更小的模型
ollama pull qwen2.5:3b-instruct-q4_K_M

# 2. 或使用云端
LOCAL_LLM_PRIORITY=0
```

---

## 📈 监控和统计

### 1. 实时监控

```python
import requests

# 获取 One-API 状态
health = requests.get("http://localhost:8001/health").json()
print(f"可用提供商: {health['available_providers']}")
print(f"本地 LLM: {'启用' if health['local_llm_enabled'] else '禁用'}")
```

### 2. 成本统计

```python
# 记录每次调用
def track_cost(provider, model):
    if provider == "local":
        cost = 0
    elif provider == "groq":
        cost = 0
    elif provider == "zhipu":
        cost = 0.001
    elif provider == "claude":
        cost = 0.01
    
    return cost
```

---

## 🎯 总结

**核心优势：**
1. ✅ **统一接口** - 一个 API 调用所有模型
2. ✅ **智能路由** - 自动选择最优提供商
3. ✅ **成本优化** - 本地推理零成本
4. ✅ **高可用** - 自动 Fallback 保证可用性
5. ✅ **灵活配置** - 支持多种策略

**适用场景：**
- 个人开发者（降低成本）
- 企业应用（保证可用性）
- 离线环境（完全本地）
- 混合部署（本地+云端）

**下一步：**
1. 在 Windows 本地部署
2. 配置 Podman 容器
3. 测试不同策略
4. 监控成本和性能

---

**项目仓库：** https://github.com/DannyFish-11/ufo-galaxy  
**最新提交：** b3145e8  
**完成时间：** 2026-01-21
